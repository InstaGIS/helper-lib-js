describe("IGProviders Entities", function () {
    it('IGProviders.ButtonFactory should be if type object', function () {
        expect(typeof IGProviders.ButtonFactory).toBe('object');
    });
    it('IGProviders.Wkt should be if type function', function () {
        expect(typeof IGProviders.Wkt).toBe('function');
    });
    it('IGProviders.Wicket should be if type function', function () {
        expect(typeof IGProviders.Wicket).toBe('function');
    });
    it('IGProviders.WKT2Object should be if type function', function () {
        expect(typeof IGProviders.WKT2Object).toBe('function');
    });
    it('IGProviders.loadingcircle should be if type function', function () {
        expect(typeof IGProviders.loadingcircle).toBe('function');
    });
    it('IGProviders.colorset should be if type object', function () {
        expect(typeof IGProviders.colorset).toBe('object');
    });
    it('IGProviders.setModalClass should be if type function', function () {
        expect(typeof IGProviders.setModalClass).toBe('function');
    });
    it('IGProviders.isArray should be if type function', function () {
        expect(typeof IGProviders.isArray).toBe('function');
    });
    it('IGProviders.spaceString should be if type function', function () {
        expect(typeof IGProviders.spaceString).toBe('function');
    });
    it('IGProviders.cleanString should be if type function', function () {
        expect(typeof IGProviders.cleanString).toBe('function');
    });
    it('IGProviders.randomname should be if type function', function () {
        expect(typeof IGProviders.randomname).toBe('function');
    });
    it('IGProviders.getCookie should be if type function', function () {
        expect(typeof IGProviders.getCookie).toBe('function');
    });
});
